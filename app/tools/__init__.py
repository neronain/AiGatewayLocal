"""Client-tools mirror: fetch, verify, stage and promote third-party downloads.

See ``app/tools/sync.py`` for the engine and ``app/tools/cli.py`` for the
``python -m app.tools`` command. The curated registry is ``config/tools.yaml``.
"""
